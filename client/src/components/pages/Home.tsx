import Header from "../Header/Header";
import MovingCards from "../Header/MovingCards";

const Home = () => {
  return (
    <div>
      <Header />
      another text
    </div>
  );
};

export default Home;
